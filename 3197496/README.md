# AWS AgentCore Chatbot for Zendesk Support

Zendesk Support용 AI 기반 챗봇 앱으로, AWS AgentCore(Amazon Bedrock)와 연동됩니다.

## 📋 기능

- **티켓 요약**: 현재 티켓의 내용을 자동으로 요약
- **응답 제안**: AI가 고객에게 보낼 전문적인 응답 제안
- **감성 분석**: 고객의 감정 상태와 긴급도 분석
- **티켓 컨텍스트 인식**: 현재 티켓 정보를 AI에 전달하여 상황에 맞는 응답 생성
- **응답 삽입**: AI 응답을 티켓 댓글에 바로 삽입

## 🏗 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                        Zendesk Support                          │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    Ticket Sidebar                          │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │              AWS AgentCore Chatbot                   │  │  │
│  │  │                    (iframe)                          │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │  Proxy Server   │  (선택사항)
                    │   (Node.js)     │
                    └─────────────────┘
                              │
                              ▼
              ┌───────────────────────────────┐
              │         AWS Cloud             │
              │  ┌─────────────────────────┐  │
              │  │   Amazon Bedrock        │  │
              │  │     AgentCore           │  │
              │  │  ┌─────────────────┐   │  │
              │  │  │   Your Agent    │   │  │
              │  │  └─────────────────┘   │  │
              │  └─────────────────────────┘  │
              └───────────────────────────────┘
```

## 🚀 시작하기

### 사전 요구사항

1. **AWS 계정**
   - Amazon Bedrock 서비스 활성화
   - AgentCore에 Agent 생성 및 배포

2. **Zendesk 계정**
   - Zendesk Support 관리자 권한
   - 앱 개발 권한

3. **개발 도구**
   - Node.js 18 이상
   - Zendesk CLI (ZCLI)

### AWS Bedrock Agent 설정

1. [AWS Bedrock Console](https://console.aws.amazon.com/bedrock/)에서 Agent 생성
2. Agent에 적절한 프롬프트 및 Knowledge Base 설정
3. Agent ID와 Agent Alias ID 기록

### Zendesk 앱 설치

#### 방법 1: ZCLI를 통한 로컬 개발

```bash
# ZCLI 설치
npm install -g @zendesk/zcli

# 앱 검증
zcli apps:validate

# 로컬 서버 시작
zcli apps:server

# Zendesk에서 테스트
# https://your-subdomain.zendesk.com/agent?zcli_apps=true
```

#### 방법 2: 패키지 업로드

```bash
# 앱 패키징
zcli apps:package

# Zendesk Admin > Apps > Manage > Upload App
# 생성된 .zip 파일 업로드
```

### 프록시 서버 설정 (선택사항)

CORS 문제 또는 보안 강화가 필요한 경우:

```bash
cd server

# 환경 변수 설정
cp .env.example .env
# .env 파일에 AWS 자격 증명 입력

# 의존성 설치
npm install

# 서버 시작
npm start
```

## ⚙️ 설정

앱 설치 시 다음 정보를 입력해야 합니다:

| 설정 | 설명 | 예시 |
|------|------|------|
| AWS Region | Bedrock Agent가 배포된 리전 | `us-east-1` |
| AWS Access Key ID | IAM 사용자 액세스 키 | `AKIA...` |
| AWS Secret Access Key | IAM 사용자 시크릿 키 | `wJalr...` |
| Agent ID | Bedrock Agent 고유 ID | `ABCD1234` |
| Agent Alias ID | Agent Alias 고유 ID | `TSTALIASID` |

### IAM 권한

앱에서 사용하는 IAM 사용자에게 다음 권한이 필요합니다:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "bedrock:InvokeAgent"
      ],
      "Resource": [
        "arn:aws:bedrock:*:*:agent/*",
        "arn:aws:bedrock:*:*:agent-alias/*"
      ]
    }
  ]
}
```

## 📁 프로젝트 구조

```
zendesk-helper-app/
├── manifest.json           # Zendesk 앱 설정
├── assets/
│   ├── iframe.html        # 앱 메인 HTML
│   ├── styles.css         # 스타일시트
│   ├── main.js            # 앱 로직
│   ├── logo.svg           # 앱 아이콘 (80x80)
│   └── logo-small.svg     # 앱 아이콘 (32x32)
├── translations/
│   ├── en.json            # 영어 번역
│   └── ko.json            # 한국어 번역
├── server/                # 프록시 서버 (선택)
│   ├── index.js
│   ├── package.json
│   └── .env.example
└── README.md
```

## 🎨 커스터마이징

### 프롬프트 수정

`assets/main.js`의 `handleQuickAction` 함수에서 퀵 액션 프롬프트를 수정할 수 있습니다:

```javascript
case 'summarize':
  prompt = 'Please summarize this ticket...';
  break;
```

### 스타일 수정

`assets/styles.css`에서 CSS 변수를 수정하여 테마를 변경할 수 있습니다:

```css
:root {
  --color-accent-primary: #FF9900;  /* 주요 색상 */
  --color-bg-primary: #0f1419;      /* 배경 색상 */
}
```

## 🔧 문제 해결

### CORS 오류

프록시 서버를 사용하거나, AWS API Gateway를 통해 CORS 설정을 구성하세요.

### 인증 오류

1. IAM 자격 증명이 올바른지 확인
2. IAM 사용자에게 필요한 권한이 있는지 확인
3. Agent ID와 Alias ID가 정확한지 확인

### 앱이 로드되지 않음

1. 브라우저 콘솔에서 오류 확인
2. `zcli apps:validate`로 앱 검증
3. Zendesk 앱 로그 확인

## 📄 라이선스

MIT License

## 🤝 기여

이슈와 풀 리퀘스트를 환영합니다!

